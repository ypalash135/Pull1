<h1>USERS</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <a href="<%= users_path(user) %>">
        <%= user.name %>
      </a>
    </li>
  <% end %>
</ul>
<li>

<%= link_to "New USER", new_user_path %>
